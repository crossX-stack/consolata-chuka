services:
  - type: web
    name: consolata-backend
    runtime: php
    buildCommand: "composer install && php artisan migrate --force"
    startCommand: "php artisan serve --host 0.0.0.0 --port 10000"
